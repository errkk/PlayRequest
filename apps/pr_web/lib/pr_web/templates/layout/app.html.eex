<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

    <meta property="og:title" content="PlayRequest"/>
    <meta property="og:description" content="🎶 An inclusive shared playlist – it's Played by Many 🥰"/>
    <%= unless assigns[:now_playing] do %>
    <meta property="og:image" content="<%= PRWeb.Endpoint.url() <> Routes.static_path(@conn, "/images/pr.png") %>"/>
    <% end %>
    <meta name="twitter:site" content="PlayRequest"/>
    <meta name="twitter:card" content="summary"/>

    <%= if assigns[:now_playing] do %>
    <meta property="og:image" content="<%= @now_playing.img %>"/>
    <meta name="twitter:label1" content="Now Playing"/>
    <meta name="twitter:data1" content="<%= @now_playing.name %>"/>
    <meta name="twitter:label2" content="By"/>
    <meta name="twitter:data2" content="<%= @now_playing.artist %>"/>
    <% end %>

    <link href="https://fonts.googleapis.com/css?family=Raleway:400,500,700|Source+Sans+Pro:400,600,700&display=swap" rel="stylesheet">
    <title>PlayRequest</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <link rel="shortcut icon" href="<%= Routes.static_path(@conn, "/images/favicon.png") %>"/>
  </head>
  <body>
    <div class="content">
      <header class="container container--header">
        <div class="col--header">
          <%= live_render(@conn, PRWeb.UserHeaderLive, session: %{
            user_id: if(assigns[:current_user], do: @current_user.id)
          }) %>
        </div>
        <div class="col--logo">
          <%= live_render(@conn, PRWeb.LogoLive) %>
        </div>
      </header>
      <main role="main">
        <div class="alert-container">
          <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
          <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
        </div>
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div>
    <footer class="footer">
      <p class="version">
        Version: <%= @version %>
      </p>
    </footer>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
